
services:
  music_eventsim:
    build:
      context: ./eventsim
      dockerfile: Dockerfile
    container_name: music_eventsim
    networks:
      - music-net
    depends_on:
      music_kafka:
        condition: service_healthy
    command: >
      -c examples/example-config.json
      --start-time "2026-01-01T00:00:00"
      --end-time "2026-12-31T23:59:59"
      --nusers 20
      --growth-rate 0
      --kafkaBrokerList music_kafka:29092
      --continuous
    environment:
      EVENTSIM_LOG_LEVEL: "INFO"
    restart: on-failure
  music_zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    container_name: music_zookeeper
    restart: unless-stopped
    networks:
      - music-net
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
      KAFKA_OPTS: "-Dzookeeper.4lw.commands.whitelist=ruok"
    volumes:
      - ./volumes/zookeeper/data:/var/lib/zookeeper/data
      - ./volumes/zookeeper/log:/var/lib/zookeeper/log
    mem_limit: 256m
    healthcheck:
      test: ["CMD", "bash", "-c", "echo 'ruok' | nc localhost 2181 | grep imok"]
      interval: 10s
      timeout: 5s
      retries: 5
  music_kafka:
    image: confluentinc/cp-kafka:7.6.0
    container_name: music_kafka
    restart: unless-stopped
    networks:
      - music-net
    depends_on:
      music_zookeeper:
        condition: service_healthy
    ports:
      - "9094:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: music_zookeeper:2181
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,EXTERNAL://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://music_kafka:29092,EXTERNAL://localhost:9094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_NUM_PARTITIONS: 3
      KAFKA_HEAP_OPTS: "-Xmx512m -Xms256m"
    volumes:
      - ./volumes/kafka:/var/lib/kafka/data
    mem_limit: 768m
    healthcheck:
      test: ["CMD", "bash", "-c", "nc -z localhost 29092"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s
  music_apicurio:
    image: apicurio/apicurio-registry-mem:latest-release
    container_name: music_apicurio
    restart: unless-stopped
    networks:
      - music-net
    depends_on:
      music_kafka:
        condition: service_healthy
    ports:
      - "8095:8080"
    mem_limit: 512m
  music_kafka_ui:
    image: provectuslabs/kafka-ui:latest
    container_name: music_kafka_ui
    restart: unless-stopped
    networks:
      - music-net
    depends_on:
      music_kafka:
        condition: service_healthy
      music_apicurio:
        condition: service_started
    ports:
      - "8091:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: music-stream-cluster
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: music_kafka:29092
      KAFKA_CLUSTERS_0_SCHEMAREGISTRY: http://music_apicurio:8080/apis/registry/v2
    mem_limit: 512m
  music_postgres:
    image: postgres:13
    container_name: music_postgres
    restart: unless-stopped
    networks:
      - music-net
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
    volumes:
      - ./volumes/postgres:/var/lib/postgresql/data
    mem_limit: 256m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U airflow"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
  music_airflow_init:
    image: apache/airflow:2.8.1
    container_name: music_airflow_init
    networks:
      - music-net
    depends_on:
      music_postgres:
        condition: service_healthy
    environment:
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@music_postgres/airflow
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        airflow db migrate &&
        airflow users create \
          --username admin \
          --password admin \
          --firstname Admin \
          --lastname User \
          --role Admin \
          --email admin@musicstream.io
    restart: "no"
  music_airflow_webserver:
    build:
      context: .
      dockerfile: airflow/Dockerfile.airflow
    container_name: music_airflow_webserver
    restart: unless-stopped
    networks:
      - music-net
    depends_on:
      music_postgres:
        condition: service_healthy
      music_airflow_init:
        condition: service_completed_successfully
    ports:
      - "8084:8080"
    environment:
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@music_postgres/airflow
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__WEBSERVER__SECRET_KEY: "musicstream-secret-key-change-in-prod"
      AIRFLOW_CONN_GOOGLE_CLOUD_DEFAULT: "google-cloud-platform://?extra__google_cloud_platform__project=${GCP_PROJECT_ID}&extra__google_cloud_platform__key_path=${AIRFLOW_GCP_KEYFILE_PATH_IN_CONTAINER}"
      GCP_PROJECT_ID: "${GCP_PROJECT_ID}"
      GCP_BQ_DATASET_NAME: "${GCP_BQ_DATASET_NAME}"
      AIRFLOW_GCP_KEYFILE_PATH_IN_CONTAINER: "${AIRFLOW_GCP_KEYFILE_PATH_IN_CONTAINER}"
    volumes:
      - ./airflow/dags:/opt/airflow/dags
      - ./airflow/logs:/opt/airflow/logs
      - ./airflow/plugins:/opt/airflow/plugins
      - ./config:/opt/airflow/dags/config
      - ./keys:/opt/airflow/keys:ro
      - ./dbt_music_stream:/opt/dbt
    user: "1000:0"
    mem_limit: 2.5G
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    command: webserver
  music_airflow_scheduler:
    build:
      context: .
      dockerfile: airflow/Dockerfile.airflow
    container_name: music_airflow_scheduler
    restart: unless-stopped
    networks:
      - music-net
    depends_on:
      music_postgres:
        condition: service_healthy
      music_airflow_init:
        condition: service_completed_successfully
    environment:
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@music_postgres/airflow
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__WEBSERVER__SECRET_KEY: "musicstream-secret-key-change-in-prod"
      AIRFLOW_CONN_GOOGLE_CLOUD_DEFAULT: "google-cloud-platform://?extra__google_cloud_platform__project=${GCP_PROJECT_ID}&extra__google_cloud_platform__key_path=${AIRFLOW_GCP_KEYFILE_PATH_IN_CONTAINER}"
      GCP_PROJECT_ID: "${GCP_PROJECT_ID}"
      GCP_BQ_DATASET_NAME: "${GCP_BQ_DATASET_NAME}"
      AIRFLOW_GCP_KEYFILE_PATH_IN_CONTAINER: "${AIRFLOW_GCP_KEYFILE_PATH_IN_CONTAINER}"
    volumes:
      - ./airflow/dags:/opt/airflow/dags
      - ./airflow/logs:/opt/airflow/logs
      - ./airflow/plugins:/opt/airflow/plugins
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config:/opt/airflow/dags/config
      - ./keys:/opt/airflow/keys:ro
      - ./dbt_music_stream:/opt/dbt
    user: "1000:0"
    group_add:
      - "984"
    mem_limit: 512m
    command: scheduler
  music_spark_master:
    image: apache/spark:3.5.1
    container_name: music_spark_master
    hostname: music-spark-master
    restart: unless-stopped
    networks:
      - music-net
    environment:
      - SPARK_MASTER_HOST=music-spark-master
      - SPARK_LOCAL_IP=music-spark-master
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - GOOGLE_APPLICATION_CREDENTIALS=/opt/spark/keys/key.json
    ports:
      - "7077:7077"
      - "8083:8080"
    volumes:
      - ./spark/jobs:/opt/spark/jobs
      - ./schemas:/opt/spark/schemas
      - ./spark/libs:/opt/spark/libs
      - ./keys:/opt/spark/keys
      - ./spark/libs:/opt/spark/libs
    mem_limit: 1G
    command: ["/opt/spark/bin/spark-class", "org.apache.spark.deploy.master.Master"]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://music_spark_master:8080 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 180s

  music_spark_worker:
    image: apache/spark:3.5.1
    container_name: music_spark_worker
    restart: unless-stopped
    networks:
      - music-net
    depends_on:
      music_spark_master:
        condition: service_healthy
    environment:
      - SPARK_MASTER=spark://music-spark-master:7077
      - SPARK_WORKER_MEMORY=2G
      - SPARK_WORKER_CORES=2
      - SPARK_LOCAL_IP=music_spark_worker
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - GOOGLE_APPLICATION_CREDENTIALS=/opt/spark/keys/key.json
    volumes:
      - ./spark/jobs:/opt/spark/jobs
      - ./schemas:/opt/spark/schemas
      - ./spark/libs:/opt/spark/libs
      - ./keys:/opt/spark/keys
    mem_limit: 2.5G
    command: ["/opt/spark/bin/spark-class", "org.apache.spark.deploy.worker.Worker", "spark://music-spark-master:7077"]

networks:
  music-net:
    driver: bridge